name: gus_tutorial

python_env: python_env.yaml

entry_points:
  main:
    parameters:
      alpha: {type: float, default: 0.5}
      l1_ratio: {type: float, default: 0.1}
      target_var: {type: str, default: "quality"}
    command: "python train.py {alpha} {l1_ratio} {target_var}"